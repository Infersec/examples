{
  "name": "infersec-mastra-web-research",
  "version": "1.0.0",
  "description": "A Mastra web research agent powered by a model hosted on Infersec.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "build:deploy": "mastra build",
    "dev": "mastra dev",
    "start": "npm run build && node --env-file=.env dist/run.js"
  },
  "dependencies": {
    "@mastra/core": "^1.50.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "mastra": "^1.18.2",
    "typescript": "^7.0.2"
  }
}
